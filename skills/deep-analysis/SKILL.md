---
name: deep-analysis
description: 深度需求分析。单命令 /deep-analysis <需求> 走完 需求澄清→GitHub调研+前模拟→可行性整合→可选对抗→闭环报告。内置 researcher/simulator 双模拟。触发:复杂/多模块/需调研的功能。简单需求(单页面/单接口/改文案)不触发。
argument-hint: <需求描述>
---

# Deep Analysis

单命令走完「需求 → 调研 → 可行性 → 闭环报告」。

## 触发判断

复杂需求才执行，满足任一即复杂：
- 多模块 / 需要架构决策
- 涉及外部集成(支付/登录/第三方 API)
- 需要调研竞品 / 复用候选 / 技术选型
- 存在规模/并发/安全风险
- 用户说"做个 XX 系统/平台"，范围模糊

**简单需求不执行**，直接给 1 句方案。

## 流程总览

```
/deep-analysis <需求>
  ├─ Step 1  需求澄清(1-2 问 → 锁定范围)── 用户确认
  ├─ Step 2  researcher → 前模拟 顺序执行
  │          ┌──────────────┐  ┌──────────────┐
  │          │ researcher   │→ │  前模拟       │
  │          │ GitHub 调研   │  │ 发现未知遗漏   │
  │          │ 产出施工素材   │  │ 遗漏→施工动作  │
  │          └──────────────┘  └──────────────┘
  ├─ Step 3  可行性整合(复用候选 + 遗漏清单)── 收尾必问: 方向 OK? 要不要跑对抗?
  ├─ Step 3.5 对抗 red-team(用户选「跑」才执行)
  └─ Step 4  三阶段: 草案→用户确认→后模拟→用户确认→写 CLOSURE.md
        ⤴ 任意时刻冒出新需求 → 暂停，按硬规则 4 处理
```

**双模拟分工**：
- **前模拟（Step 2）**：发现你不知道自己不知道的事 → 遗漏清单
- **后模拟（闭环自检，Step 4）**：验证计划覆盖了所有已知需求 → 覆盖表 + 依赖检查 + 来源诚实

## Step 1: 需求澄清

问 1-2 个关键问题（平台？规模？已有代码还是全新？），锁定范围后用户确认。产出 `phase0`（JSON 格式见 `stages/simulator.md`）。

## Step 2: 调研 + 前模拟（顺序执行）

基于 phase0，主 Agent 顺序执行两个阶段（不 spawn 子代理，产物留在对话内传递）：

- **researcher** — **Read `stages/researcher.md`**，GitHub 调研同类项目，产出施工素材（复用候选 + 施工信息）
- **前模拟** — **Read `stages/simulator.md`**，基于 phase0 + researcher 产物，模拟实现过程扫遗漏，每项遗漏配施工动作

顺序执行 → 前模拟拿到 researcher 的复用候选再模拟，零重复搜索、信息充分。

## Step 3: 可行性整合

researcher + 前模拟产物返回后，主 Agent 整合输出：复用候选 Top3 表 + 遗漏 Top5 表 + 裁决（proceed/改/转向/kill）。输出模板见 **`stages/redteam.md`**。

⚠️ **Step 3 强制收尾问题（必须问，答完才进 Step 4）**——方向确认 + 对抗决策一次问完：

> **"方向对吗？可行性 OK 吗？要不要跑对抗 red-team？（跑 / 跳过）"**

即使用户回答「直接下一步」/「确认」，**也必须追问对抗决策**（跑 / 跳过），得到明确回答后才进 Step 4。

### Step 3.5: 对抗 red-team（在写闭环报告之前执行）

用户选「对抗」时执行：**Read `stages/redteam.md`**。6 角色攻击 + 攻击矩阵输出。对抗结果如有新发现，喂给 Step 4 闭环报告。

⚠️ **Step 3.5 强制收尾（必须问，答完才进 Step 4）**——汇报对抗结果后必须问：

> **"对抗结果 OK 吗？要调整方案/范围吗？确认后我写闭环报告。"**

得到用户明确确认后才写闭环报告。

用户选「跳过」→ 直接进 Step 4。

## Step 4: 输出闭环报告

⚠️ **前置硬约束：必须在 Step 3 完成对抗决策询问（跑/跳过）、且 Step 3.5 对抗结果获用户确认之后才开始 Step 4**。未确认就开始 = 违规。

**Read `stages/closure.md`** 执行三阶段（4a 草案 → 4b 后模拟 → 4c 写 CLOSURE.md）。

⚠️ **Step 4a 强制收尾**——发完草案必须问用户确认：

> **"方案 OK 吗？确认后我跑后模拟验证。"**

## 硬规则

1. **复杂需求才触发** — 简单需求直接忽略
2. **复用优先** — 先搜 GitHub，有现成直接引用，不从零想方案
3. **每条声明标注来源** — GitHub 代码(repo@文件:行号) 或 「LLM 推测」
4. **需求变更即停** — 任意时刻冒出新需求（流程中/闭环报告后）：暂停 → 记录 → 评估影响 → 回退对应 Step（动范围→Step1、动方案→Step2、仅追加→闭环报告需求池）→ 用户确认后继续。绝不默默按旧需求做到底

## 目录结构

```
~/.claude/skills/deep-analysis/
├── SKILL.md              # 本文件: 编排 + 4 Step + 可选对抗 + 强制收尾问题
└── stages/
    ├── researcher.md      # 调研: GitHub 搜索 + 施工素材
    ├── simulator.md       # phase0 + 前模拟: 遗漏扫描 + 施工动作
    ├── redteam.md         # Step 3 可行性整合模板 + Step 3.5 对抗
    └── closure.md         # Step 4 闭环报告: 草案 + 后模拟 + 写文件
```